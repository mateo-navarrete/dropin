import React, { Fragment as F } from 'react';
import { LogInForm } from './LogInForm';

export const View = props => {
  // console.log(props);
  return (
    <F>
      <LogInForm />
    </F>
  );
};
