//jscs:disable requireShorthandArrowFunctions
import React from 'react';
import { Button } from '../material';

const MenuButton = props => {
  return <Button color="inherit">Login</Button>;
};

export const ToggleMenuButton = MenuButton;
