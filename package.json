{
  "name": "dropin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./backend/bin/www",
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "local-start": "concurrently --kill-others \"cd frontend && npm start\" \"cd backend && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "repository": {},
  "bugs": {},
  "homepage": "",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
